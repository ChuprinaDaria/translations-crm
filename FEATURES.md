# 🚀 Нові фічі CRM системи

Цей документ описує 5 нових модульних фіч, які значно покращують продуктивність менеджерів.

## 📋 Зміст

1. [Command Palette (God Mode)](#1-command-palette-god-mode)
2. [Smart Paste](#2-smart-paste)
3. [Context Sidebar](#3-context-sidebar)
4. [Drag-to-Upload](#4-drag-to-upload)
5. [Аудіо-нотатки](#5-аудіо-нотатки)

---

## 1. Command Palette (God Mode) ⌘K

### Опис
Швидкий доступ до всіх дій через командну палітру. Натисніть `Cmd+K` (Mac) або `Ctrl+K` (Windows/Linux) для відкриття.

### Функціонал
- **Створення замовлення** - швидкий доступ до форми створення
- **Пошук клієнтів** - швидкий перехід до картки клієнта
- **Пошук замовлень** - швидкий перехід до замовлення
- **Зміна статусу** - швидка зміна статусу поточного замовлення

### Використання
1. Натисніть `Cmd+K` або `Ctrl+K`
2. Введіть назву клієнта, замовлення або команду
3. Оберіть потрібну дію зі списку

### Технічні деталі
- **Модуль**: `src/modules/command-palette/`
- **Компонент**: `CommandPalette.tsx`
- **Хук**: `useCommandPalette.ts`
- **Бібліотека**: `cmdk` (вже встановлена)

---

## 2. Smart Paste 📋

### Опис
Інтелектуальний парсер тексту з буфера обміну. Автоматично розпізнає та заповнює поля форми замовлення.

### Функціонал
Автоматично розпізнає:
- Ім'я клієнта
- Телефон та email
- Дата та час події
- Кількість осіб
- Мова перекладу
- Тип оплати
- Локація
- Формат заходу

### Використання
1. Скопіюйте текст заявки з мейлу/WhatsApp
2. Відкрийте форму створення замовлення
3. Натисніть кнопку "Вставити з буфера"
4. Поля автоматично заповняться

### Технічні деталі
- **Модуль**: `src/modules/smart-paste/`
- **Компонент**: `SmartPasteButton.tsx`
- **API**: `POST /api/v1/smart-paste/parse-order`
- **Бекенд**: `app/modules/smart_paste/router.py`

### Приклад тексту для парсингу
```
Клієнт: Олександр Петренко
Телефон: +380501234567
Email: alex@example.com
Дата: 15.02.2024
Час: 18:00
Кількість осіб: 50
Мова: англійська
Оплата: готівка
```

---

## 3. Context Sidebar 📱

### Опис
Контекстна панель, яка автоматично відкривається при виборі розмови в Inbox. Показує всю інформацію про клієнта та його замовлення.

### Функціонал
- Інформація про клієнта (ім'я, телефон, email)
- Активні замовлення клієнта
- Історія оплат
- Швидка кнопка створення замовлення

### Використання
1. Відкрийте розділ "Inbox"
2. Оберіть розмову з клієнтом
3. Справа автоматично відкриється панель з контекстом
4. Перегляньте активні замовлення або створіть нове

### Технічні деталі
- **Модуль**: `src/modules/context-sidebar/`
- **Компонент**: `ContextSidebar.tsx`
- **Інтеграція**: Автоматично в `InboxPage.tsx`

---

## 4. Drag-to-Upload 📎

### Опис
Перетягування файлів прямо на картку замовлення в Kanban дошці без відкриття форми.

### Функціонал
- Перетягування PDF, зображень, документів
- Автоматичне завантаження файлу
- Опціональна зміна статусу на "DO_POSWIADCZENIA"
- Візуальна індикація перетягування

### Використання
1. Відкрийте Kanban дошку
2. Перетягніть файл на картку замовлення
3. Файл автоматично завантажиться
4. Статус може автоматично змінитися (опціонально)

### Технічні деталі
- **Модуль**: `src/modules/drag-upload/`
- **Компонент**: `DraggableCard.tsx`
- **API**: `POST /api/v1/drag-upload/upload`
- **Бекенд**: `app/modules/drag_upload/router.py`
- **Бібліотека**: `react-dropzone`

### Підтримувані формати
- PDF (`.pdf`)
- Зображення (`.png`, `.jpg`, `.jpeg`)
- Документи (`.doc`, `.docx`)

---

## 5. Аудіо-нотатки 🎤

### Опис
Запис голосових нотаток з автоматичним перетворенням в текст через Whisper API.

### Функціонал
- Запис аудіо через браузер
- Автоматична транскрипція в текст
- Збереження тексту в поле опису замовлення
- Підтримка української мови

### Використання
1. Відкрийте деталі замовлення
2. Натисніть кнопку "Голосова нотатка"
3. Дозвольте доступ до мікрофона
4. Говоріть вашу нотатку
5. Натисніть "Зупинити запис"
6. Текст автоматично з'явиться в полі приміток

### Технічні деталі
- **Модуль**: `src/modules/audio-notes/`
- **Компонент**: `AudioNoteButton.tsx`
- **API**: `POST /api/v1/audio-notes/transcribe`
- **Бекенд**: `app/modules/audio_notes/router.py`
- **Сервіс**: OpenAI Whisper API

### Налаштування
Для роботи потрібно додати в змінні оточення:
```bash
OPENAI_API_KEY=your-openai-api-key
```

---

## 🛠 Встановлення та налаштування

### Фронтенд

1. Встановіть залежності:
```bash
cd translations-crm
npm install
```

2. Запустіть dev сервер:
```bash
npm run dev
```

### Бекенд

1. Встановіть Python залежності:
```bash
cd app
pip install -r requirements.txt
```

2. Додайте змінні оточення в `.env`:
```env
OPENAI_API_KEY=your-openai-api-key
```

3. Запустіть сервер:
```bash
uvicorn main:app --reload
```

---

## 📁 Структура модулів

```
src/modules/
├── command-palette/      # Command Palette
│   ├── components/
│   ├── hooks/
│   └── index.ts
├── smart-paste/          # Smart Paste
│   ├── components/
│   ├── api/
│   └── index.ts
├── context-sidebar/      # Context Sidebar
│   ├── components/
│   └── index.ts
├── drag-upload/          # Drag-to-Upload
│   ├── components/
│   ├── api/
│   └── index.ts
└── audio-notes/          # Аудіо-нотатки
    ├── components/
    ├── api/
    └── index.ts

app/modules/
├── smart_paste/          # Smart Paste API
│   └── router.py
├── drag_upload/         # Drag Upload API
│   └── router.py
└── audio_notes/          # Audio Notes API
    └── router.py
```

---

## 🎯 WOW-ефекти

1. **Command Palette**: Менеджер відчуває себе хакером, швидкість роботи зростає в 3 рази
2. **Smart Paste**: "Воно само заповнилось!" 🤯
3. **Context Sidebar**: Менеджер не забуває контекст розмови. Все перед очима
4. **Drag-to-Upload**: Економія 3-х кліків на кожному файлі
5. **Аудіо-нотатки**: Менеджери перестануть "губити" усні домовленості

---

## 🔧 Розширення

Всі модулі побудовані модульно і легко розширюються:

- Додайте нові команди в Command Palette
- Покращте парсинг в Smart Paste
- Додайте нові поля в Context Sidebar
- Розширте підтримку форматів в Drag-to-Upload
- Додайте інші мови в Аудіо-нотатки

---

## 📝 Примітки

- Всі модулі незалежні і можуть використовуватися окремо
- Бекенд API endpoints готові до використання
- Для Whisper API потрібен OpenAI API ключ
- Всі компоненти використовують Shadcn/UI для консистентного дизайну

