# Налаштування інтеграцій: InPost та Przelewy24

## 📦 InPost - Налаштування доставки

### Які дані потрібні:

1. **API Key (Organization Token)** - основний ключ для роботи з API
   - Для production: `api_key`
   - Для тестування: `sandbox_api_key`

2. **Webhook URL** (опціонально) - URL для отримання оновлень статусу
   - Формат: `https://your-domain.com/api/v1/postal-services/inpost/webhook`

3. **Webhook Secret** (опціонально) - секретний ключ для перевірки webhook запитів
   - Можна згенерувати випадковий рядок

4. **Дані відправника за замовчуванням** (опціонально):
   - `default_sender_name` - назва компанії/відправника
   - `default_sender_email` - email відправника
   - `default_sender_phone` - телефон відправника

### Де взяти дані:

#### 1. Реєстрація та отримання API ключа:

1. **Зареєструйтеся в InPost:**
   - Перейдіть на https://inpost.pl/
   - Створіть акаунт або увійдіть в існуючий

2. **Створіть Organization:**
   - У панелі InPost створіть Organization (організацію)
   - Це необхідно для отримання API доступу

3. **Отримайте API ключ:**
   - Перейдіть в **API Settings** або **Ustawienia API**
   - Згенеруйте **Organization Token** (це і є ваш API Key)
   - Скопіюйте ключ - він використовується як `api_key`

4. **Для тестування (Sandbox):**
   - Увімкніть Sandbox режим в панелі InPost
   - Отримайте Sandbox API Key
   - Використовуйте його як `sandbox_api_key`

#### 2. Налаштування Webhook (опціонально):

1. **В панелі InPost:**
   - Перейдіть в **API Settings → Webhooks**
   - Додайте новий webhook:
     - **URL:** `https://your-domain.com/api/v1/postal-services/inpost/webhook`
     - **Secret:** згенеруйте випадковий рядок (наприклад, через генератор паролів)
     - **Events:** виберіть всі події статусів (`status.updated`, `shipment.updated`)

2. **В CRM системі:**
   - Введіть той самий URL та Secret в налаштуваннях InPost

### Де ввести дані в CRM:

1. Відкрийте **Налаштування → InPost** (вкладка в Settings)
2. Заповніть поля:
   - ✅ **Production API Key** - вставте ваш Organization Token
   - ✅ **Sandbox режим** - увімкніть для тестування
   - ✅ **Sandbox API Key** - якщо використовуєте sandbox
   - ✅ **Webhook URL** - URL вашого сервера
   - ✅ **Webhook Secret** - той самий, що в панелі InPost
   - ✅ **Дані відправника** - заповніть за потреби
3. Увімкніть інтеграцію (чекбокс "Увімкнути InPost інтеграцію")
4. Збережіть налаштування

### Корисні посилання:

- **InPost API Documentation:** https://documentation-points-api.easypack24.net/
- **InPost ShipX API:** https://api-shipx-pl.easypack24.net/v1/
- **InPost Sandbox:** https://sandbox-api-shipx-pl.easypack24.net/v1/
- **Підтримка InPost:** https://inpost.pl/pomoc
- **Email підтримки API:** api@inpost.pl

---

## 💳 Przelewy24 - Налаштування оплати

### Які дані потрібні:

1. **Merchant ID** - ID вашого магазину в Przelewy24
   - Це числовий ідентифікатор

2. **POS ID** - ID точки продажу (Point of Sale)
   - Зазвичай співпадає з Merchant ID, але може бути іншим

3. **CRC Key** - ключ для підпису транзакцій
   - Використовується для безпеки та верифікації платежів

4. **API Key (Klucz do raportów)** - ключ для доступу до API
   - Використовується для авторизації API запитів

5. **Sandbox режим** - для тестування
   - Увімкніть для роботи з тестовим середовищем

### Де взяти дані:

#### 1. Реєстрація та отримання даних:

1. **Зареєструйтеся в Przelewy24:**
   - Перейдіть на https://www.przelewy24.pl/
   - Створіть бізнес-акаунт
   - Пройдіть верифікацію (може знадобитися час)

2. **Отримайте дані в панелі:**
   - Увійдіть в панель: https://panel.przelewy24.pl
   - Перейдіть в **"Moje konto"** → **"Moje dane"** → **"Dane API i konfiguracja"**
   - Там ви знайдете:
     - **Merchant ID** (ID Sprzedawcy)
     - **POS ID** (ID Punktu Sprzedaży)
     - **CRC** (Klucz CRC)
     - **API Key** (Klucz do raportów / Secret ID)

3. **Для тестування (Sandbox):**
   - Перейдіть на https://sandbox.przelewy24.pl
   - Створіть тестовий акаунт
   - Отримайте тестові дані (Merchant ID, POS ID, CRC, API Key)
   - Увімкніть `przelewy24_sandbox = true` в налаштуваннях

#### 2. Налаштування Webhook (опціонально):

1. **В панелі Przelewy24:**
   - Перейдіть в **"Konfiguracja"** → **"URL statusu"**
   - Введіть URL: `https://your-domain.com/api/v1/payment/webhooks/przelewy24`
   - Збережіть

2. **IP Whitelist (рекомендовано):**
   - Додайте IP адресу вашого сервера в whitelist
   - Це забезпечить додаткову безпеку

### Де ввести дані в CRM:

1. Відкрийте **Налаштування → Przelewy24** (вкладка в Settings)
2. Заповніть поля:
   - ✅ **Sandbox режим** - увімкніть для тестування
   - ✅ **Merchant ID** - вставте ваш Merchant ID
   - ✅ **POS ID** - вставте ваш POS ID
   - ✅ **CRC Key** - вставте ваш CRC ключ
   - ✅ **API Key** - вставте ваш API Key (Klucz do raportów)
3. Увімкніть інтеграцію (чекбокс "Увімкнути Przelewy24")
4. Збережіть налаштування

### Тестування з'єднання:

Після введення даних використовуйте кнопку **"Тестувати з'єднання"** для перевірки правильності налаштувань.

### Корисні посилання:

- **Przelewy24 Panel:** https://panel.przelewy24.pl
- **Przelewy24 Sandbox:** https://sandbox.przelewy24.pl
- **Przelewy24 API Documentation:** https://www.przelewy24.pl/dokumentacja
- **Підтримка Przelewy24:** через панель або email

---

## 💳 Stripe - Налаштування оплати

### Які дані потрібні:

1. **Publishable Key (pk_...)** - публічний ключ для фронтенду
2. **Secret Key (sk_...)** - секретний ключ для бекенду
3. **Webhook Secret (whsec_...)** - секретний ключ для верифікації webhook

### Де взяти дані:

1. **Зареєструйтеся в Stripe:**
   - Перейдіть на https://stripe.com
   - Створіть акаунт або увійдіть в існуючий

2. **Отримайте API ключі:**
   - Перейдіть в **Developers** → **API keys**
   - Скопіюйте **Publishable key** (pk_test_... або pk_live_...)
   - Скопіюйте **Secret key** (sk_test_... або sk_live_...)

3. **Налаштуйте Webhook:**
   - Перейдіть в **Developers** → **Webhooks**
   - Натисніть **Add endpoint**
   - Введіть URL: `https://your-domain.com/api/v1/payment/webhooks/stripe`
   - Виберіть події:
     - `checkout.session.completed`
     - `payment_intent.succeeded`
     - `payment_intent.payment_failed`
   - Скопіюйте **Signing secret** (whsec_...)

### Де ввести дані в CRM:

1. Відкрийте **Налаштування → Payment Settings**
2. Заповніть поля Stripe:
   - ✅ **Public Key** - вставте ваш Publishable key
   - ✅ **Secret Key** - вставте ваш Secret key
   - ✅ **Webhook Secret** - вставте ваш Webhook signing secret
3. Увімкніть інтеграцію (чекбокс "Увімкнути Stripe")
4. Збережіть налаштування

### Тестування з'єднання:

Після введення даних використовуйте кнопку **"Тестувати з'єднання"** для перевірки правильності налаштувань.

### Корисні посилання:

- **Stripe Dashboard:** https://dashboard.stripe.com
- **Stripe API Documentation:** https://stripe.com/docs/api
- **Stripe Testing:** https://stripe.com/docs/testing

---

## 🔔 Webhook URL - Для чого він потрібен?

### Що таке Webhook?

**Webhook** - це механізм, який дозволяє зовнішнім сервісам (InPost, Przelewy24) автоматично надсилати повідомлення вашому серверу про зміни в реальному часі.

### Навіщо потрібен Webhook URL?

#### Для InPost:

**Без Webhook:**
- ❌ Вам потрібно вручну перевіряти статус відправлення через API
- ❌ Статуси оновлюються тільки коли ви самі запитуєте їх
- ❌ Можна пропустити важливі оновлення (доставлено, проблеми з доставкою)

**З Webhook:**
- ✅ InPost автоматично надсилає повідомлення при зміні статусу
- ✅ Статуси оновлюються миттєво в вашій CRM системі
- ✅ Ви отримуєте сповіщення про:
  - Підтвердження відправлення
  - Відправлення в дорозі
  - Готовність до отримання
  - Доставку
  - Проблеми з доставкою
  - Повернення відправнику

**Приклад роботи:**
```
1. Ви створюєте відправлення через CRM
2. InPost підтверджує відправлення
3. InPost надсилає webhook: "status.updated" → "confirmed"
4. Ваша CRM автоматично оновлює статус
5. Клієнт отримує відправлення
6. InPost надсилає webhook: "status.updated" → "delivered"
7. Ваша CRM автоматично позначає як доставлено
```

#### Для Przelewy24:

**Без Webhook:**
- ❌ Потрібно вручну перевіряти статус платежу
- ❌ Можна пропустити успішну оплату
- ❌ Клієнт може оплатити, але ви не дізнаєтесь

**З Webhook:**
- ✅ Przelewy24 автоматично надсилає повідомлення про оплату
- ✅ Статус платежу оновлюється миттєво
- ✅ Автоматично створюється запис у Finance модулі
- ✅ Ви отримуєте сповіщення про:
  - Успішну оплату
  - Невдалу оплату
  - Скасовану оплату
  - Повернення коштів

**Приклад роботи:**
```
1. Клієнт переходить на сторінку оплати Przelewy24
2. Клієнт оплачує замовлення
3. Przelewy24 надсилає webhook: "payment completed"
4. Ваша CRM автоматично:
   - Оновлює статус платежу на "completed"
   - Створює запис у Finance модулі
   - Оновлює статус замовлення
5. Ви бачите оплату в системі без додаткових дій
```

### Як працює Webhook URL?

1. **Ви налаштовуєте URL** в панелі провайдера (InPost/Przelewy24)
2. **Провайдер надсилає HTTP POST запит** на ваш URL при події
3. **Ваш сервер обробляє запит** та оновлює дані в базі
4. **Система автоматично синхронізується** без вашої участі

### Формат Webhook URL:

**Для InPost:**
```
https://your-domain.com/api/v1/postal-services/inpost/webhook
```

**Для Przelewy24:**
```
https://your-domain.com/api/v1/payment/webhooks/przelewy24
```

**Для Stripe:**
```
https://your-domain.com/api/v1/payment/webhooks/stripe
```

**Важливо:**
- ✅ URL повинен бути доступний з інтернету (не localhost)
- ✅ Обов'язково використовуйте HTTPS (безпека)
- ✅ URL повинен бути стабільним (не змінюватися)

### Чи обов'язковий Webhook?

**Ні, webhook не обов'язковий**, але **дуже рекомендується**:

- ✅ **З Webhook:** автоматичне оновлення, миттєві сповіщення, менше ручної роботи
- ⚠️ **Без Webhook:** потрібно вручну перевіряти статуси, можна пропустити оновлення

### Як налаштувати Webhook?

1. **Отримайте публічний URL** вашого сервера (наприклад, `https://crm.example.com`)
2. **Додайте шлях** до webhook endpoint:
   - InPost: `/api/v1/postal-services/inpost/webhook`
   - Przelewy24: `/api/v1/payment/webhooks/przelewy24`
   - Stripe: `/api/v1/payment/webhooks/stripe`
3. **Введіть повний URL** в панелі провайдера
4. **Введіть той самий URL** в налаштуваннях CRM
5. **Налаштуйте Webhook Secret** для безпеки (рекомендовано)

### Безпека Webhook:

- 🔐 **Webhook Secret** - перевіряє що запит дійсно від провайдера
- 🔒 **HTTPS** - захищає дані при передачі
- ✅ **Signature verification** - автоматична перевірка підпису запиту

---

## 🔒 Безпека

### Рекомендації:

1. **Ніколи не публікуйте API ключі** в публічних репозиторіях
2. **Використовуйте HTTPS** для всіх webhook URL
3. **Регулярно оновлюйте ключі** якщо є підозра на компрометацію
4. **Використовуйте Sandbox** для тестування перед production
5. **Перевіряйте webhook signatures** для безпеки

### Шифрування:

- API ключі зберігаються зашифрованими в базі даних
- Webhook secrets перевіряються при кожному запиті
- Чутливі дані не відображаються в API відповідях

---

## ❓ Часті питання

### InPost:

**Q: Де знайти Organization Token?**  
A: В панелі InPost → API Settings → Organization Token

**Q: Чи потрібен webhook?**  
A: Не обов'язково, але рекомендується для автоматичного оновлення статусів

**Q: Як тестувати без реальних відправлень?**  
A: Увімкніть Sandbox режим та використовуйте sandbox API key

### Przelewy24:

**Q: Merchant ID та POS ID однакові?**  
A: Зазвичай так, але можуть відрізнятися залежно від налаштувань акаунту

**Q: Де знайти CRC ключ?**  
A: В панелі Przelewy24 → Moje konto → Moje dane → Dane API i konfiguracja

**Q: Як тестувати платежі?**  
A: Використовуйте Sandbox режим та тестові дані з sandbox.przelewy24.pl

**Q: Webhook не працює?**  
A: Перевірте:
- URL доступний з інтернету (HTTPS)
- IP адреса в whitelist (якщо налаштовано)
- Правильність URL в панелі Przelewy24

---

## 📞 Підтримка

Якщо виникли проблеми з налаштуванням:

1. Перевірте правильність введених даних
2. Переконайтесь що використовуєте правильні ключі (test/live)
3. Перевірте логи системи на наявність помилок
4. Зверніться до підтримки провайдера:
   - **InPost:** api@inpost.pl
   - **Przelewy24:** через панель керування

---

*Останнє оновлення: 2026-02-04*

