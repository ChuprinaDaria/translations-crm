name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  # Перевірка перед деплоєм
  check:
    name: Check Frontend & Backend
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install frontend dependencies
      run: npm ci
    
    - name: TypeScript type check
      run: |
        if [ -f "tsconfig.json" ]; then
          npx tsc --noEmit
        else
          echo "⚠️ No tsconfig.json found, skipping TypeScript check"
        fi
    
    - name: Build frontend
      run: npm run build
    
    - name: Verify frontend build
      run: |
        if [ ! -d "build" ]; then
          echo "❌ Error: Build directory not found!"
          exit 1
        fi
        if ! grep -r "MenuManagement" build/assets/ > /dev/null 2>&1; then
          echo "⚠️ Warning: MenuManagement not found in build"
        fi
        echo "✅ Frontend build verified!"
    
    - name: Check backend files
      run: |
        if [ ! -f "app/main.py" ]; then
          echo "❌ Error: Backend main.py not found!"
          exit 1
        fi
        if [ ! -f "app/requirements.txt" ]; then
          echo "❌ Error: Backend requirements.txt not found!"
          exit 1
        fi
        echo "✅ Backend files verified!"

  # Деплой на сервер
  deploy:
    name: Deploy to Server
    needs: check
    runs-on: ubuntu-latest
    
    steps:
    - name: 🚀 Deploy via SSH
      uses: appleboy/ssh-action@v1.0.0
      env:
        POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
        POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
        POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
        JWT_SECRET: ${{ secrets.JWT_SECRET }}
        APP_ENV: ${{ secrets.APP_ENV }}
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.SSH_PORT }}
        envs: POSTGRES_USER,POSTGRES_PASSWORD,POSTGRES_DB,JWT_SECRET,APP_ENV
        script: |
          set -e
          cd /opt/cafe-crm
          
          echo "📥 Pulling latest code..."
          # Не змінюємо remote URL - використовуємо той що вже налаштований
          # Якщо на сервері репозиторій клонований через HTTPS - залишаємо HTTPS
          # Якщо через SSH - залишаємо SSH
          
          # Додаємо GitHub до known_hosts на всяк випадок (для SSH)
          mkdir -p ~/.ssh
          ssh-keyscan -H github.com >> ~/.ssh/known_hosts 2>/dev/null || true
          
          # Використовуємо fetch + reset
          git fetch origin main || {
            echo "⚠️ Git fetch failed. Checking remote configuration..."
            git remote -v
            echo "ℹ️ If using HTTPS, ensure repository is public or configure access token"
            echo "ℹ️ If using SSH, ensure SSH key is configured on server"
            exit 1
          }
          git reset --hard origin/main
          
          echo "📝 Creating .env file from secrets..."
          cat > .env << ENVFILE
          JWT_SECRET=${JWT_SECRET}
          POSTGRES_USER=${POSTGRES_USER:-postgres}
          POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
          POSTGRES_DB=${POSTGRES_DB:-cafe_db}
          APP_ENV=${APP_ENV:-production}
          ENVFILE
          
          echo "🐳 Rebuilding backend..."
          docker-compose build backend
          
          echo "▶️ Restarting services..."
          docker-compose up -d
          
          echo "⏳ Waiting for backend to be healthy..."
          sleep 10
          
          echo "🔍 Checking backend health..."
          if docker ps | grep -q cafe_backend; then
            echo "✅ Backend container is running"
          else
            echo "❌ Backend container is not running!"
            docker ps -a | grep cafe
            exit 1
          fi
          
          echo "🔨 Building frontend..."
          rm -rf build/
          NODE_OPTIONS=--max_old_space_size=1536 npm run build
          
          echo "✅ Checking build..."
          if grep -r "MenuManagement" build/assets/ > /dev/null; then
            echo "✅ Frontend build complete!"
          else
            echo "⚠️ Frontend build might be incomplete"
          fi
          
          echo "🔄 Restarting nginx..."
          sudo docker restart whiteout_nginx
          
          echo "🎉 Deploy finished at $(date)"
          echo "📊 Running containers:"
          docker ps --filter "name=cafe" --format "table {{.Names}}\t{{.Status}}"
