# Повний опис поточного стану CRM системи

## 📋 Загальний огляд

**Тип проекту:** Повнофункціональна веб-платформа CRM (Customer Relationship Management)

**Технології:**
- **Frontend:** React 18 + TypeScript
- **Styling:** Tailwind CSS v4.0
- **Build tool:** Vite
- **Backend API:** https://mdev.alwaysdata.net
- **Аутентифікація:** JWT + TOTP (двофакторна)

**Тема дизайну:**
- Світла тема з білим фоном
- Акцентний колір: `#FF5A00` (помаранчевий)
- Відступи: 16px / 24px
- SaaS-стиль інтерфейс
- 12-колонкова розкладка

---

## 🏗️ Архітектура проекту

### Структура файлів

```
/
├── App.tsx                          # Головний компонент (роутинг, auth)
├── vite.config.ts                   # Vite конфігурація + CORS proxy
├── package.json                     # Залежності проекту
├── tsconfig.json                    # TypeScript конфігурація
│
├── /components/                     # React компоненти
│   ├── Dashboard.tsx               # Головна панель з KPI картками
│   ├── Header.tsx                  # Верхня панель (breadcrumbs, профіль)
│   ├── Sidebar.tsx                 # Ліва навігаційна панель
│   ├── ItemsManagement.tsx         # Управління товарами
│   ├── CategoriesManagement.tsx    # Управління категоріями
│   ├── MenuManagement.tsx          # Управління меню
│   ├── CreateKP.tsx                # Створення комерційних пропозицій
│   ├── KPArchive.tsx               # Архів КП
│   ├── KPTemplates.tsx             # Шаблони КП
│   ├── KPICard.tsx                 # Карточка KPI метрики
│   ├── InfoTooltip.tsx             # Tooltip компонент
│   │
│   ├── /auth/                      # Аутентифікація
│   │   ├── AuthPage.tsx            # Обгортка для логіну/реєстрації
│   │   ├── LoginForm.tsx           # Форма входу з TOTP
│   │   └── RegisterForm.tsx        # Форма реєстрації з QR кодом
│   │
│   ├── /ui/                        # 15 кастомних UI компонентів
│   │   ├── button.tsx              # Кнопка
│   │   ├── input.tsx               # Поле введення
│   │   ├── select.tsx              # Випадаючий список
│   │   ├── dialog.tsx              # Модальне вікно
│   │   ├── alert-dialog.tsx        # Діалог підтвердження
│   │   ├── card.tsx                # Картка
│   │   ├── table.tsx               # Таблиця
│   │   ├── badge.tsx               # Значок
│   │   ├── switch.tsx              # Перемикач
│   │   ├── textarea.tsx            # Текстове поле
│   │   ├── label.tsx               # Мітка
│   │   ├── alert.tsx               # Повідомлення
│   │   ├── tabs.tsx                # Вкладки
│   │   ├── dropdown-menu.tsx       # Випадаюче меню
│   │   └── ... (ще ~30 компонентів)
│   │
│   └── /figma/
│       └── ImageWithFallback.tsx   # Компонент для зображень
│
├── /lib/
│   └── api.ts                      # API клієнт (всі запити до backend)
│
├── /styles/
│   └── globals.css                 # Глобальні стилі + Tailwind токени
│
├── /types/
│   └── global.d.ts                 # TypeScript типи
│
└── /guidelines/
    └── Guidelines.md               # Гайдлайни проекту
```

---

## 🎨 Дизайн система

### Кольори

**Основні:**
- Background: `#FFFFFF` (білий)
- Primary (акцент): `#FF5A00` (помаранчевий)
- Text: `#1F2937` (темно-сірий)
- Secondary text: `#6B7280` (сірий)
- Border: `#E5E7EB` (світло-сірий)

**Статуси:**
- Success: `#10B981` (зелений)
- Error: `#EF4444` (червоний)
- Warning: `#F59E0B` (жовтий)
- Info: `#3B82F6` (синій)

### Типографіка

Визначена в `/styles/globals.css`:
- H1: 32px, напівжирний
- H2: 24px, напівжирний
- H3: 20px, напівжирний
- Body: 16px, нормальний
- Small: 14px, нормальний

**ВАЖЛИВО:** Не використовуються Tailwind класи для розміру шрифту, ваги та висоти рядка (вони задані глобально).

### Відступи

- Малий: `16px`
- Великий: `24px`
- Сітка: 12 колонок
- Gap між елементами: `16px` / `24px`

---

## 🔐 Система аутентифікації

### Технологія: JWT + TOTP (Google Authenticator)

### 1. Реєстрація (`/components/auth/RegisterForm.tsx`)

**Що відбувається:**

1. **Крок 1: Заповнення форми**
   - Користувач вводить email та пароль
   - Підтверджує пароль (має співпадати)
   - Валідація на клієнті:
     - Email формат (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`)
     - Пароль мінімум 6 символів
     - Паролі співпадають

2. **Крок 2: Індикатор надійності пароля**
   - Слабкий (червоний): < 8 символів
   - Середній (жовтий): 8+ символів
   - Хороший (помаранчевий): 8+ символів + цифри
   - Сильний (зелений): 12+ символів + велика/мала літера + цифри + спецсимволи

3. **Крок 3: Відправка на сервер**
   - `POST /api/auth/register`
   - Body: `{ email, password }`
   - Response: `{ id, email, is_active, is_admin, created_at, otpauth_url }`

4. **Крок 4: Генерація QR коду**
   - Використовується бібліотека `qrcode`
   - `QRCode.toDataURL(otpauth_url)` → генерує base64 зображення
   - QR код містить секрет для Google Authenticator

5. **Крок 5: Екран з QR кодом**
   - Показується великий QR код (248x248px)
   - Інструкція з 5 кроків:
     1. Відкрийте Google Authenticator або Authy
     2. Натисніть "Додати акаунт" або "+"
     3. Виберіть "Сканувати QR код"
     4. Наведіть камеру на QR код
     5. Збережіть 6-значний код
   - Кнопка "Продовжити до логіну"

**UI особливості:**
- Skeleton loader під час генерації QR коду
- Alert з іконкою смартфона
- Зелена галочка успіху
- Показується email користувача

---

### 2. Логін (`/components/auth/LoginForm.tsx`)

**Що відбувається:**

1. **Крок 1: Заповнення форми**
   - Email
   - Пароль
   - **6-значний код з Google Authenticator**
     - Поле з `maxLength={6}`
     - Автоматична фільтрація (тільки цифри)
     - Великий шрифт, відцентрований, з tracking
     - Placeholder: "000000"

2. **Крок 2: Валідація на клієнті**
   - Всі поля обов'язкові
   - Код має бути рівно 6 цифр

3. **Крок 3: Відправка на сервер**
   - `POST /api/auth/login`
   - Body: `{ email, password, code }`
   - **Response: plain text string (токен)**
     - НЕ JSON об'єкт!
     - Просто: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

4. **Крок 4: Збереження токену**
   - `localStorage.setItem('auth_token', token)`
   - Токен автоматично додається до всіх наступних запитів

5. **Крок 5: Редирект**
   - На головну сторінку (`/dashboard`)
   - Користувач побачить CRM систему

**UI особливості:**
- Loading spinner під час входу
- Помилки показуються в Alert (червоний)
- Підказка під полем коду
- Кнопка "Немає акаунту? Зареєструватися"

---

### 3. Захист роутів (`/App.tsx`)

**Логіка:**

```typescript
const [isAuthenticated, setIsAuthenticated] = useState(
  tokenManager.isAuthenticated()
);

// Якщо не авторизований → показати AuthPage
if (!isAuthenticated) {
  return <AuthPage onAuthSuccess={handleAuthSuccess} />;
}

// Якщо авторизований → показати CRM
return (
  <div className="flex h-screen">
    <Sidebar />
    <div className="flex-1 flex flex-col">
      <Header />
      <main>{/* Content based on currentPage */}</main>
    </div>
  </div>
);
```

**Перевірка токену:**
- При завантаженні додатку перевіряється `localStorage.getItem('auth_token')`
- Якщо токену немає → показується форма логіну
- Якщо токен є → показується CRM
- При отриманні 401 з сервера → токен видаляється і редирект на логін

---

### 4. Вихід з системи

**Функція:**
```typescript
const handleLogout = () => {
  tokenManager.removeToken(); // Видалити з localStorage
  setIsAuthenticated(false);  // Оновити стан
  // → Автоматично показується AuthPage
};
```

**UI:**
- Dropdown меню в Header (клік на аватар)
- Пункт "Вийти" з іконкою LogOut
- Після виходу показується форма логіну

---

## 📦 Управління товарами (`/components/ItemsManagement.tsx`)

### Можливості:

1. **Перегляд списку товарів**
2. **Створення нового товару**
3. **Редагування товару**
4. **Видалення товару**
5. **Пошук та фільтрація**
6. **Пагінація**

---

### 1. Перегляд списку товарів

**Відображення:**
- Таблиця з колонками:
  - Фото (52x52px)
  - Назва
  - Категорія → Підкатегорія
  - Ціна (₴)
  - Вага + одиниця
  - Статус (активний/неактивний)
  - Дії (редагувати/видалити)

**Функція завантаження:**
```typescript
const loadData = async () => {
  setLoading(true);
  try {
    const [itemsData, categoriesData, subcategoriesData] = await Promise.all([
      itemsApi.getItems(currentPage * itemsPerPage, itemsPerPage),
      categoriesApi.getCategories(),
      subcategoriesApi.getSubcategories(),
    ]);
    
    setItems(itemsData);
    setCategories(categoriesData);
    setSubcategories(subcategoriesData);
  } catch (error) {
    toast.error("Помилка завантаження даних");
  } finally {
    setLoading(false);
  }
};
```

**Порожній стан:**
- Коли немає товарів показується:
  - Іконка Package
  - "Додайте перший товар"
  - "Почніть з створення вашого першого товару у системі"
  - Кнопка "Додати товар"

**Loading стан:**
- Skeleton таблиця з 5 рядками
- Анімація pulse

---

### 2. Створення товару

**UI:** Modal (Dialog) з формою

**Поля:**

| Поле | Тип | Обов'язкове | Опис |
|------|-----|-------------|------|
| Назва товару | Input (text) | ✅ | Назва товару |
| Опис | Textarea | ❌ | Детальний опис |
| Категорія | Select | ✅ | Вибір з списку категорій |
| Підкатегорія | Select | ✅ | Фільтрується по категорії |
| Ціна (₴) | Input (number) | ✅ | Ціна товару |
| Вага | Input (number) | ❌ | Вага товару |
| Одиниця виміру | Select | ❌ | г, кг, мл, л, шт |
| URL фото | Input (url) | ❌ | Посилання на фото |
| Активний | Switch | ✅ | Чи показувати товар |

**Валідація:**
```typescript
const validateForm = () => {
  if (!formData.name.trim()) {
    toast.error("Введіть назву товару");
    return false;
  }
  
  if (formData.price <= 0) {
    toast.error("Ціна повинна бути більше 0");
    return false;
  }
  
  if (!formData.subcategory_id) {
    toast.error("Виберіть підкатегорію");
    return false;
  }
  
  return true;
};
```

**Динамічна фільтрація підкатегорій:**
```typescript
// При виборі категорії фільтруються підкатегорії
const filteredSubcategories = selectedCategory
  ? subcategories.filter(sub => sub.category_id === selectedCategory)
  : subcategories;
```

**Процес створення:**
1. Користувач заповнює форму
2. Клік "Створити"
3. Валідація на клієнті
4. `POST /api/items` з даними
5. Сервер повертає створений товар
6. Товар додається в список: `setItems([...items, newItem])`
7. Toast: "Товар успішно створено!"
8. Modal закривається
9. Форма очищується

**Код:**
```typescript
const handleCreate = async () => {
  if (!validateForm()) return;
  
  setLoading(true);
  try {
    const newItem = await itemsApi.createItem(formData);
    setItems([...items, newItem]);
    setIsCreateModalOpen(false);
    resetForm();
    toast.success("Товар успішно створено!");
  } catch (error: any) {
    toast.error(error.data?.detail || "Помилка створення товару");
  } finally {
    setLoading(false);
  }
};
```

---

### 3. Редагування товару

**UI:** Той самий Modal, але з заповненими даними

**Процес:**
1. Клік на іконку Pencil біля товару
2. `setEditingItem(item)` - зберегли товар
3. `setFormData(item)` - заповнили форму
4. `setIsEditModalOpen(true)` - відкрили modal
5. Користувач змінює дані
6. Клік "Зберегти"
7. `PUT /api/items/${item.id}` з оновленими даними
8. Сервер повертає оновлений товар
9. Товар оновлюється в списку:
   ```typescript
   setItems(items.map(i => i.id === updatedItem.id ? updatedItem : i))
   ```
10. Toast: "Товар успішно оновлено!"

**Код:**
```typescript
const handleUpdate = async () => {
  if (!validateForm() || !editingItem) return;
  
  setLoading(true);
  try {
    const updatedItem = await itemsApi.updateItem(editingItem.id, formData);
    setItems(items.map(item => 
      item.id === editingItem.id ? updatedItem : item
    ));
    setIsEditModalOpen(false);
    setEditingItem(null);
    resetForm();
    toast.success("Товар успішно оновлено!");
  } catch (error: any) {
    toast.error(error.data?.detail || "Помилка оновлення товару");
  } finally {
    setLoading(false);
  }
};
```

---

### 4. Видалення товару

**UI:** AlertDialog з підтвердженням

**Процес:**
1. Клік на іконку Trash2 біля товару
2. `setItemToDelete(item)` - зберегли товар
3. `setIsDeleteDialogOpen(true)` - відкрили dialog
4. Dialog показує:
   - Заголовок: "Ви впевнені?"
   - Текст: "Ця дія не може бути скасована. Товар буде видалено назавжди."
   - Кнопки: "Скасувати" | "Видалити"
5. Якщо "Видалити":
6. `DELETE /api/items/${item.id}`
7. Товар видаляється зі списку:
   ```typescript
   setItems(items.filter(i => i.id !== itemToDelete.id))
   ```
8. Toast: "Товар успішно видалено!"

**Код:**
```typescript
const handleDelete = async () => {
  if (!itemToDelete) return;
  
  setLoading(true);
  try {
    await itemsApi.deleteItem(itemToDelete.id);
    setItems(items.filter(item => item.id !== itemToDelete.id));
    setIsDeleteDialogOpen(false);
    setItemToDelete(null);
    toast.success("Товар успішно видалено!");
  } catch (error: any) {
    toast.error(error.data?.detail || "Помилка видалення товару");
  } finally {
    setLoading(false);
  }
};
```

---

### 5. Пошук та фільтрація

**Пошук:**
```typescript
const [searchQuery, setSearchQuery] = useState("");

// Фільтрація по назві
const filteredItems = items.filter(item =>
  item.name.toLowerCase().includes(searchQuery.toLowerCase())
);
```

**UI пошуку:**
- Input з іконкою Search
- Placeholder: "Шукати товар..."
- Real-time пошук (без кнопки)

**Фільтрація по категорії:**
```typescript
const [categoryFilter, setCategoryFilter] = useState<number | null>(null);

const filtered = items.filter(item => {
  const matchesSearch = item.name.toLowerCase().includes(searchQuery);
  const matchesCategory = !categoryFilter || 
    item.subcategory?.category_id === categoryFilter;
  
  return matchesSearch && matchesCategory;
});
```

**UI фільтрів:**
- Select з категоріями
- Option "Всі категорії"

---

### 6. Пагінація

**Логіка:**
```typescript
const [currentPage, setCurrentPage] = useState(0);
const itemsPerPage = 20;

// Завантаження при зміні сторінки
useEffect(() => {
  loadData();
}, [currentPage]);

// Кнопки
<Button onClick={() => setCurrentPage(p => p - 1)} disabled={currentPage === 0}>
  Попередня
</Button>
<span>Сторінка {currentPage + 1}</span>
<Button onClick={() => setCurrentPage(p => p + 1)}>
  Наступна
</Button>
```

**API запит:**
```typescript
itemsApi.getItems(currentPage * itemsPerPage, itemsPerPage);
// skip = 0, limit = 20 (перша сторінка)
// skip = 20, limit = 20 (друга сторінка)
// skip = 40, limit = 20 (третя сторінка)
```

---

## 📂 Управління категоріями (`/components/CategoriesManagement.tsx`)

### Структура: Категорії → Підкатегорії

**Приклад:**
```
Закуски
  ├── Холодні закуски
  ├── Гарячі закуски
  └── Канапе
  
Супи
  ├── М'ясні супи
  ├── Овочеві супи
  └── Крем-супи
```

---

### 1. Перегляд категорій

**Відображення:**
- Список категорій з підкатегоріями
- Кожна категорія - карточка
- Підкатегорії показуються під категорією
- Іконки: FolderOpen (категорія), Tag (підкатегорія)

**Структура UI:**
```
[Категорія: Закуски]  [Редагувати] [Видалити]
  └─ Холодні закуски  [Редагувати] [Видалити]
  └─ Гарячі закуски   [Редагувати] [Видалити]
  └─ [+ Додати підкатегорію]
```

---

### 2. Створення категорії

**UI:** Modal з одним полем

**Поля:**
- Назва категорії (обов'язкове)

**Процес:**
1. Клік "Додати категорію"
2. Modal відкривається
3. Введення назви
4. `POST /api/categories` з `{ name }`
5. Категорія додається в список
6. Toast: "Категорію створено!"

---

### 3. Створення підкатегорії

**UI:** Modal з двома полями

**Поля:**
- Назва підкатегорії (обов'язкове)
- Батьківська категорія (select, автоматично вибрана)

**Процес:**
1. Клік "+ Додати підкатегорію" під категорією
2. Modal відкривається з pre-selected категорією
3. Введення назви
4. `POST /api/subcategories` з `{ name, category_id }`
5. Підкатегорія додається під категорією
6. Toast: "Підкатегорію створено!"

---

### 4. Редагування та видалення

Аналогічно до товарів:
- Редагування через modal
- Видалення з підтвердженням
- Toast повідомлення

---

## 🎯 Dashboard (`/components/Dashboard.tsx`)

### KPI карточки

**4 основні метрики:**

1. **Активні КП**
   - Іконка: FileText (синя)
   - Значення: число
   - Зміна: +12% vs попередній період
   - Trend: зелена стрілка вгору

2. **Товарів в меню**
   - Іконка: ShoppingBag (помаранчева)
   - Значення: число
   - Зміна: +5% vs попередній період
   - Trend: зелена стрілка вгору

3. **Загальна вартість**
   - Іконка: DollarSign (зелена)
   - Значення: ₴ сума
   - Зміна: +18% vs попередній період
   - Trend: зелена стрілка вгору

4. **Завершені КП**
   - Іконка: CheckCircle2 (фіоле��ова)
   - Значення: число
   - Зміна: -3% vs попередній період
   - Trend: червона стрілка вниз

**Компонент KPICard:**
```typescript
<KPICard
  title="Активні КП"
  value={24}
  change={12}
  icon={<FileText />}
  iconColor="bg-blue-100 text-blue-600"
/>
```

---

### Швидкі дії

**4 кнопки:**
1. Створити КП
2. Додати товар
3. Переглянути архів
4. Управління меню

**UI:**
- Іконка + текст
- Hover ефект
- Клік → навігація на відповідну сторінку

---

### Останні КП (майбутня функція)

Таблиця з останніми комерційними пропозиціями:
- Номер КП
- Клієнт
- Дата створення
- Сума
- Статус
- Дії

**Статуси:**
- Чернетка (сірий badge)
- Відправлено (синій badge)
- Схвалено (зелений badge)
- Відхилено (червоний badge)

---

## 🧩 Навігація

### Sidebar (`/components/Sidebar.tsx`)

**Розмір:** ~260px фіксована ширина

**Структура:**

```
[Logo] CRM Система

КОМЕРЦІЙНА ПРОПОЗИЦІЯ
  □ Dashboard
  □ Створити КП
  □ Шаблони КП
  □ Архів КП

УПРАВЛІННЯ
  □ Меню
  □ Товари
  □ Категорії

ПРОДАЖІ (disabled)
  □ Лідси
  □ Контакти
  □ Звіти

СЕРВІС (disabled)
  □ Заявки
  □ Обладнання
  □ Графік
```

**Активний пункт:**
- Фон: `bg-[#FF5A00]/10`
- Текст: `text-[#FF5A00]`
- Бордер: ліва помаранчева лінія

**Hover:**
- Фон: `bg-gray-100`

**Disabled пункти:**
- Opacity: 50%
- Cursor: not-allowed
- Tooltip: "Доступно незабаром"

---

### Header (`/components/Header.tsx`)

**Структура:**

```
[Breadcrumbs]                    [Пошук] [Оновити] [Профіль ▼]
```

**Breadcrumbs:**
- Home > Поточна сторінка
- Автоматично генерується з `currentPage`
- Іконка Home для головної

**Пошук (майбутнє):**
- Input з іконкою Search
- Глобальний пошук по системі

**Кнопка оновити:**
- Іконка RefreshCw
- Перезавантажує дані поточної сторінки

**Профіль:**
- Аватар (ініціали користувача)
- Ім'я користувача
- Dropdown меню:
  - Профіль
  - Налаштування
  - Вийти

---

## 🛠️ Кастомні UI компоненти

Всього **15 головних компонентів** в `/components/ui/`:

### 1. Button (`button.tsx`)

**Варіанти:**
- `default` - помаранчева з білим текстом
- `destructive` - червона
- `outline` - з обводкою
- `secondary` - сіра
- `ghost` - прозора
- `link` - як посилання

**Розміри:**
- `default` - середній
- `sm` - малий
- `lg` - великий
- `icon` - тільки іконка

**Приклад:**
```tsx
<Button variant="default" size="lg">
  Створити КП
</Button>
```

---

### 2. Input (`input.tsx`)

**Типи:**
- text, email, password, number, url, tel, search

**Стилі:**
- Бордер: сірий
- Focus: помаранчева обводка
- Error: червона обводка
- Disabled: сірий фон

**Приклад:**
```tsx
<Input 
  type="email" 
  placeholder="your@email.com"
  value={email}
  onChange={(e) => setEmail(e.target.value)}
/>
```

---

### 3. Select (`select.tsx`)

**Особливості:**
- Native HTML select (не кастомний dropdown)
- Стилізований під дизайн систему
- Option groups підтримка

**Приклад:**
```tsx
<Select value={category} onChange={handleChange}>
  <option value="">Оберіть категорію</option>
  <option value="1">Закуски</option>
  <option value="2">Супи</option>
</Select>
```

---

### 4. Dialog (`dialog.tsx`)

**Модальне вікно:**
- Overlay (темний фон)
- Content (біла карточка)
- Close button (X)
- Trap focus (не можна клікнути поза)
- ESC закриває

**Структура:**
```tsx
<Dialog open={isOpen} onOpenChange={setIsOpen}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Заголовок</DialogTitle>
      <DialogDescription>Опис</DialogDescription>
    </DialogHeader>
    
    {/* Контент */}
    
    <DialogFooter>
      <Button>Зберегти</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

---

### 5. AlertDialog (`alert-dialog.tsx`)

**Діалог підтвердження:**
- Для небезпечних дій (видалення)
- Не закривається при кліку поза
- Потребує явного підтвердження

**Приклад:**
```tsx
<AlertDialog open={isOpen} onOpenChange={setIsOpen}>
  <AlertDialogContent>
    <AlertDialogHeader>
      <AlertDialogTitle>Ви впевнені?</AlertDialogTitle>
      <AlertDialogDescription>
        Ця дія не може бути скасована.
      </AlertDialogDescription>
    </AlertDialogHeader>
    <AlertDialogFooter>
      <AlertDialogCancel>Скасувати</AlertDialogCancel>
      <AlertDialogAction onClick={handleDelete}>
        Видалити
      </AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialogContent>
</AlertDialog>
```

---

### 6. Card (`card.tsx`)

**Карточка для контенту:**
- Білий фон
- Тінь
- Округлені кути
- Padding 24px

**Структура:**
```tsx
<Card>
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
    <CardDescription>Опис</CardDescription>
  </CardHeader>
  <CardContent>
    {/* Контент */}
  </CardContent>
  <CardFooter>
    {/* Кнопки */}
  </CardFooter>
</Card>
```

---

### 7. Table (`table.tsx`)

**Таблиця:**
- Responsive (горизонтальний скрол на мобільних)
- Hover на рядках
- Striped rows (опціонально)

**Структура:**
```tsx
<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Назва</TableHead>
      <TableHead>Ціна</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>Товар 1</TableCell>
      <TableCell>₴100</TableCell>
    </TableRow>
  </TableBody>
</Table>
```

---

### 8. Badge (`badge.tsx`)

**Значок для статусів:**

**Варіанти:**
- `default` - сірий
- `success` - зелений
- `warning` - жовтий
- `destructive` - червоний
- `outline` - з обводкою

**Приклад:**
```tsx
<Badge variant="success">Активний</Badge>
<Badge variant="destructive">Неактивний</Badge>
```

---

### 9. Switch (`switch.tsx`)

**Перемикач (toggle):**
- Анімація переключення
- Disabled стан
- Checked/Unchecked

**Приклад:**
```tsx
<Switch 
  checked={isActive} 
  onCheckedChange={setIsActive}
/>
```

---

### 10. Textarea (`textarea.tsx`)

**Багаторядкове текстове поле:**
- Auto-resize (опціонально)
- Max height
- Стилі як у Input

**Приклад:**
```tsx
<Textarea 
  placeholder="Опис товару..."
  value={description}
  onChange={(e) => setDescription(e.target.value)}
  rows={4}
/>
```

---

### 11. Label (`label.tsx`)

**Мітка для полів:**
- Автоматична асоціація з input (htmlFor)
- Accessibility

**Приклад:**
```tsx
<Label htmlFor="email">Email</Label>
<Input id="email" type="email" />
```

---

### 12. Alert (`alert.tsx`)

**Повідомлення:**

**Варіанти:**
- `default` - синій (інфо)
- `destructive` - червоний (помилка)
- `success` - зелений
- `warning` - жовтий

**Приклад:**
```tsx
<Alert variant="destructive">
  <AlertCircle className="h-4 w-4" />
  <AlertTitle>Помилка</AlertTitle>
  <AlertDescription>
    Не вдалося завантажити дані
  </AlertDescription>
</Alert>
```

---

### 13. Tabs (`tabs.tsx`)

**Вкладки:**
- Горизонтальні
- Анімація переключення
- Keyboard navigation (Arrow keys)

**Приклад:**
```tsx
<Tabs defaultValue="all">
  <TabsList>
    <TabsTrigger value="all">Всі</TabsTrigger>
    <TabsTrigger value="active">Активні</TabsTrigger>
    <TabsTrigger value="archived">Архів</TabsTrigger>
  </TabsList>
  
  <TabsContent value="all">
    {/* Контент */}
  </TabsContent>
  <TabsContent value="active">
    {/* Контент */}
  </TabsContent>
</Tabs>
```

---

### 14. DropdownMenu (`dropdown-menu.tsx`)

**Випадаюче меню:**
- Автоматичне позиціонування
- Keyboard navigation
- Dividers, labels, shortcuts

**Приклад:**
```tsx
<DropdownMenu>
  <DropdownMenuTrigger asChild>
    <Button variant="ghost">
      <Avatar />
    </Button>
  </DropdownMenuTrigger>
  
  <DropdownMenuContent align="end">
    <DropdownMenuLabel>Мій акаунт</DropdownMenuLabel>
    <DropdownMenuSeparator />
    <DropdownMenuItem>Профіль</DropdownMenuItem>
    <DropdownMenuItem>Налаштування</DropdownMenuItem>
    <DropdownMenuSeparator />
    <DropdownMenuItem onClick={handleLogout}>
      Вийти
    </DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```

---

### 15. Tooltip (`tooltip.tsx`)

**Підказка при наведенні:**
- Автоматичне позиціонування
- Delay перед показом
- Arrow

**Приклад:**
```tsx
<Tooltip>
  <TooltipTrigger asChild>
    <Button variant="ghost" size="icon">
      <Info className="h-4 w-4" />
    </Button>
  </TooltipTrigger>
  <TooltipContent>
    <p>Підказка тут</p>
  </TooltipContent>
</Tooltip>
```

---

## 📡 API Client (`/lib/api.ts`)

### Архітектура

**Центральна функція:** `apiFetch<T>(endpoint, options)`

**Групи API:**
1. `authApi` - аутентифікація
2. `itemsApi` - товари
3. `categoriesApi` - категорії
4. `subcategoriesApi` - підкатегорії

---

### authApi

**Методи:**
```typescript
// Реєстрація
await authApi.register({ email, password })
// → { id, email, is_active, is_admin, created_at, otpauth_url }

// Логін
const token = await authApi.login({ email, password, code })
// → "eyJhbGc..." (plain text token)

// Logout
authApi.logout()
// → видаляє токен з localStorage
```

---

### itemsApi

**Методи:**
```typescript
// Список товарів
const items = await itemsApi.getItems(skip, limit)
// → Item[]

// Один товар
const item = await itemsApi.getItem(itemId)
// → Item

// Створити
const newItem = await itemsApi.createItem({
  name, description, price, weight, unit, 
  photo_url, active, subcategory_id
})
// → Item

// Оновити
const updated = await itemsApi.updateItem(itemId, { price: 50 })
// → Item

// Видалити
await itemsApi.deleteItem(itemId)
// → string
```

---

### categoriesApi

**Методи:**
```typescript
// Список категорій
const categories = await categoriesApi.getCategories()
// → Category[]

// Створити
const newCategory = await categoriesApi.createCategory("Десерти")
// → Category
```

---

### subcategoriesApi

**Методи:**
```typescript
// Всі підкатегорії
const subs = await subcategoriesApi.getSubcategories()
// → Subcategory[]

// Підкатегорії категорії
const subs = await subcategoriesApi.getSubcategories(categoryId)
// → Subcategory[]

// Створити
const newSub = await subcategoriesApi.createSubcategory({
  name: "Холодні закуски",
  category_id: 1
})
// → Subcategory
```

---

### tokenManager

**Методи:**
```typescript
tokenManager.getToken()        // → string | null
tokenManager.setToken(token)   // зберегти в localStorage
tokenManager.removeToken()     // видалити з localStorage
tokenManager.isAuthenticated() // → boolean
```

---

### Обробка помилок

**ApiError клас:**
```typescript
class ApiError extends Error {
  status: number;      // 401, 422, 500, ...
  statusText: string;  // "Unauthorized"
  data?: any;          // JSON з сервера
}
```

**Приклад обробки:**
```typescript
try {
  const items = await itemsApi.getItems();
} catch (error: any) {
  if (error.status === 401) {
    // Не авторизований
    tokenManager.removeToken();
    navigate('/login');
  } else if (error.status === 422) {
    // Валідація
    toast.error(error.data?.detail || "Помилка валідації");
  } else {
    // Інше
    toast.error("Щось пішло не так");
  }
}
```

---

## 🔄 Vite Config & CORS Proxy

### vite.config.ts

**Особливості:**
- React SWC (швидкий компілятор)
- TypeScript підтримка
- Alias для imports (`@/`)
- Build target: esnext
- Dev server: port 3000
- **CORS proxy для API**

**Proxy конфігурація:**
```typescript
server: {
  proxy: {
    '/api': {
      target: 'https://mdev.alwaysdata.net',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, ''),
      secure: false
    }
  }
}
```

**Як працює:**
- В development: `fetch('/api/items')` → проксується на `https://mdev.alwaysdata.net/items`
- В production: `fetch('https://mdev.alwaysdata.net/items')` → прямий запит

**API_BASE_URL:**
```typescript
const API_BASE_URL = import.meta.env?.DEV
  ? '/api'  // Development: через proxy
  : 'https://mdev.alwaysdata.net';  // Production
```

---

## 🎨 Стилі (`/styles/globals.css`)

### Tailwind v4.0

**Імпорти:**
```css
@import "tailwindcss";
```

**Кастомні токени:**
```css
@theme {
  --color-primary: #FF5A00;
  --spacing-base: 16px;
  --spacing-large: 24px;
}
```

**Typography:**
```css
h1 { font-size: 32px; font-weight: 600; }
h2 { font-size: 24px; font-weight: 600; }
h3 { font-size: 20px; font-weight: 600; }
body { font-size: 16px; font-weight: 400; }
```

**ВАЖЛИВО:** Не використовувати Tailwind класи для font-size, font-weight, line-height.

---

## 📦 Залежності (package.json)

### Основні:
```json
{
  "react": "^18.x",
  "react-dom": "^18.x",
  "typescript": "^5.x",
  "vite": "^5.x",
  "tailwindcss": "^4.x",
  "lucide-react": "^0.x",  // Іконки
  "qrcode": "^1.5.3",       // QR коди для TOTP
  "sonner": "^2.0.3"        // Toast notifications
}
```

### Dev:
```json
{
  "@vitejs/plugin-react-swc": "^3.x",
  "@types/react": "^18.x",
  "@types/react-dom": "^18.x",
  "@types/qrcode": "^1.x"
}
```

---

## 🚀 Команди

```bash
# Розробка (з hot reload)
npm run dev
# → http://localhost:3000

# Build для production
npm run build
# → створює /build/

# Preview production build
npm run preview
# → тестування build локально

# Type checking
npm run typecheck
```

---

## ✅ Що працює на 100%

### Аутентифікація ✅
- [x] Реєстрація з TOTP
- [x] QR код генерація
- [x] Логін з 6-значним кодом
- [x] Збереження токену
- [x] Автоматична авторизація
- [x] Вихід з системи
- [x] Захист роутів

### Управління товарами ✅
- [x] Перегляд списку
- [x] Створення товару
- [x] Редагування товару
- [x] Видалення товару
- [x] Пошук
- [x] Фільтрація по категорії
- [x] Пагінація

### Управління категоріями ✅
- [x] Перегляд списку
- [x] Створення категорії
- [x] Створення підкатегорії
- [x] Редагування
- [x] Видалення
- [x] Ієрархічна структура

### UI компоненти ✅
- [x] 15 кастомних компонентів
- [x] Без залежностей від сторонніх UI бібліотек
- [x] Повністю типізовані
- [x] Responsive дизайн

### API інтеграція ✅
- [x] Централізований API client
- [x] Автоматичний токен
- [x] Обробка помилок
- [x] TypeScript типи
- [x] CORS proxy

### UX ✅
- [x] Loading states
- [x] Skeleton screens
- [x] Toast notifications
- [x] Error handling
- [x] Валідація форм
- [x] Порожні стани

---

## 🔜 Що ще не реалізовано

### Комерційні пропозиції (КП)
- [ ] Створення КП
- [ ] Шаблони КП
- [ ] Архів КП
- [ ] Експорт в PDF
- [ ] Відправка email

### Продажі (disabled)
- [ ] Лідси
- [ ] Контакти
- [ ] Звіти
- [ ] CRM pipeline

### Сервіс (disabled)
- [ ] Заявки
- [ ] Обладнання
- [ ] Графік робіт

### Додаткове
- [ ] Про��іль користувача
- [ ] Налаштування системи
- [ ] Права доступу (ролі)
- [ ] Аналітика та графіки
- [ ] Експорт даних

---

## 🎯 Ролі користувачів (майбутнє)

Заплановано 5 ролей:

1. **КП Manager**
   - Створення і управління КП
   - Перегляд товарів та меню
   - Не може редагувати товари

2. **Sales Manager**
   - Управління лідами
   - Контакти клієнтів
   - Звіти по продажам

3. **Service Manager**
   - Управління заявками
   - Графік робіт
   - Обладнання

4. **Sales Lead**
   - Всі права Sales Manager
   - Управління командою продажів
   - Аналітика

5. **Service Lead**
   - Всі права Service Manager
   - Управління командою сервісу
   - Аналітика

**Примітка:** Поки що всі користувачі мають повні права (is_admin).

---

## 📱 Responsive дизайн

### Breakpoints:

- **Mobile:** < 768px
- **Tablet:** 768px - 1024px
- **Desktop:** > 1024px

### Адаптація:

**Mobile:**
- Sidebar ховається в бургер меню
- Таблиці з горизонтальним скролом
- Карточки на всю ширину
- Форми на всю ширину

**Tablet:**
- Sidebar collapse (іконки без тексту)
- Таблиці responsive
- 2-колоночна сітка

**Desktop:**
- Повний sidebar
- Таблиці на всю ширину
- 12-колоночна сітка

---

## 🔒 Безпека

### Токени
- Зберігаються в `localStorage`
- Автоматично додаються до всіх запитів
- Видаляються при виході
- **Примітка:** Для production краще httpOnly cookies

### TOTP (двофакторна аутентифікація)
- 6-значні коди
- Ротація кожні 30 секунд
- Інтеграція з Google Authenticator / Authy
- Захист від brute force (на стороні сервера)

### Валідація
- На клієнті (швидка відповідь)
- На сервері (захист від обходу)

### HTTPS
- Всі запити через HTTPS
- Secure flag для cookies (майбутнє)

---

## 📊 Статистика проекту

**Файли:**
- React компоненти: ~20
- UI компоненти: ~40
- TypeScript файли: ~65
- CSS файли: 1 (globals.css)

**Рядків коду:**
- TypeScript/TSX: ~8000
- CSS: ~500
- Config: ~100

**Функціонал:**
- Endpoints: 12
- Сторінки: 7 (з них 3 активні)
- Модалів: 8
- Форм: 10

**UI компоненти:**
- Кастомні: 15 головних + ~25 додаткових
- Сторонні: 0 (всі створені з нуля)

---

## 🐛 Відомі особливості

### API Response formats

1. **Логін повертає plain text:**
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```
   Не JSON! Обробляється спеціально в `apiFetch`.

2. **GET /items повертає масив:**
   ```json
   [{ item1 }, { item2 }]
   ```
   Не об'єкт з `{ items: [], total: 0 }`.

3. **DELETE повертає string або 204:**
   Обробляється в `apiFetch`.

### CORS в development

Використовується Vite proxy:
- `/api/*` → `https://mdev.alwaysdata.net/*`
- Автоматичний обхід CORS

---

## 📚 Документація

**Файли:**
- `/API_INTEGRATION.md` - повний гайд API
- `/API_STRUCTURE_EXPLAINED.md` - пояснення архітектури
- `/CORS_PROXY_SETUP.md` - налаштування проксі
- `/CURRENT_STATE.md` - цей документ
- `/README.md` - швидкий старт
- `/guidelines/Guidelines.md` - правила розр��бки

---

## 🎓 Як працювати з проектом

### 1. Запуск

```bash
npm install
npm run dev
```

### 2. Реєстрація

1. Відкрити http://localhost:3000
2. Клік "Зареєструватися"
3. Ввести email та пароль
4. Відсканувати QR код в Google Authenticator
5. Клік "Продовжити до логіну"

### 3. Логін

1. Ввести email та пароль
2. Ввести 6-значний код з Google Authenticator
3. Клік "Увійти"
4. → Редирект на Dashboard

### 4. Додавання товару

1. Sidebar → "Товари"
2. Клік "Додати товар"
3. Заповнити форму
4. Вибрати категорію та підкатегорію
5. Клік "Створити"
6. Toast: "Товар успішно створено!"

### 5. Вихід

1. Клік на аватар (правий верхній кут)
2. Клік "Вийти"
3. → Редирект на логін

---

## 🔧 Технічні деталі

### TypeScript

Всі компоненти повністю типізовані:
```typescript
interface Item {
  id: number;
  name: string;
  description?: string;
  price: number;
  // ...
}

const items: Item[] = await itemsApi.getItems();
```

### React Hooks використовуються

- `useState` - локальний стан
- `useEffect` - side effects (завантаження даних)
- `useMemo` - мемоізація (фільтрація)
- `useCallback` - мемоізація функцій (майбутнє)

### Патерни

**Compound components:**
```tsx
<Card>
  <CardHeader>...</CardHeader>
  <CardContent>...</CardContent>
</Card>
```

**Render props** (майбутнє):
```tsx
<DataTable renderRow={(item) => <Row item={item} />} />
```

**Controlled components:**
```tsx
<Input value={name} onChange={e => setName(e.target.value)} />
```

---

## 📈 Performance

### Оптимізації

1. **Lazy loading** (майбутнє)
2. **Code splitting** (майбутнє)
3. **Мемоізація** списків
4. **Debounce** для пошуку (майбутнє)
5. **Virtual scrolling** для великих списків (майбутнє)

### Bundle size

- Development: ~2MB (з source maps)
- Production: ~200KB (після мініфікації)

---

## 🌐 Browser support

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

**Не підтримується:**
- IE 11

---

## 📝 Висновок

Це **повнофункціональна CRM система** з:
- ✅ Безпечною аутентифікацією (JWT + TOTP)
- ✅ Повним CRUD для товарів та категорій
- ✅ Сучасним UI/UX дизайном
- ✅ TypeScript типізацією
- ✅ Responsive дизайном
- ✅ Real-time валідацією
- ✅ Централізованим API клієнтом

**Готово до розширення** з новими модулями:
- Комерційні пропозиції
- Модуль продажів
- Модуль сервісу
- Аналітика

**Технологічний стек:**
React + TypeScript + Tailwind + Vite = швидка, надійна, масштабована система.

---

**Версія документації:** 1.0  
**Дата:** 24 листопада 2024  
**Автор:** CRM Development Team
